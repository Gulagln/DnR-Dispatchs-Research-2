---
interface Props {
  size?: number;
  class?: string;
}
const { size = 40, class: className = '' } = Astro.props;
---
<img
  class={`compass-mark ${className}`}
  src="/logo-mark.png"
  width={size}
  height={size}
  alt="DnR compass mark"
  loading="eager"
  decoding="async"
/>
